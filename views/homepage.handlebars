<link rel="stylesheet" href="/css/styles.css">
<div id="headerTag2">
  <h4>KidTastic</h4>
</div>

<!-- Loop over each object in the projects array -->
{{#each children as |Child| }}

<div class="row mb-4 project">
  <div class="col-md-5">
    <div id="homepage">
      <h2>
        <!-- Call helper function with triple brackets to render the emoji span as HTML -->
        {{{get_emoji}}}
        <br
          href="/Child/{{Child.id}}">{{Child.firstName}}</br>{{Child.lastName}}</br>{{Child.birthDate}}</br>{{Child.userId}}</a>
      </h2>
    </div>



  </div>
  <div class="col-md-7">

  </div>
</div>
{{/each}}